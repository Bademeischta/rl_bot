diff --git a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.cpp b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.cpp
index cf8c7d8..09cfc26 100644
--- a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.cpp
+++ b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.cpp
@@ -29,12 +29,22 @@ namespace RLGSC {
 		return rewardFn->GetAllRewards(state, prevActions, done);
 	}
 
-	bool Match::IsDone(const GameState& state) {
-		for (auto& cond : terminalConditions)
-			if (cond->IsTerminal(state))
-				return true;
+	bool Match::IsDone(const GameState& state, bool* outTruncated) {
+		// RLbot patch (truncation): evaluate every condition so that a real terminal condition
+		// (goal) wins over a truncation (timeout) firing in the same step.
+		bool done = false;
+		bool anyTermination = false;
+		for (auto& cond : terminalConditions) {
+			if (cond->IsTerminal(state)) {
+				done = true;
+				if (!cond->IsTruncation())
+					anyTermination = true;
+			}
+		}
 
-		return false;
+		if (outTruncated)
+			*outTruncated = done && !anyTermination;
+		return done;
 	}
 
 	ScoreLine Match::GetScoreLine(const GameState& state) {
diff --git a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.h b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.h
index 3564992..b101bd1 100644
--- a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.h
+++ b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Envs/Match.h
@@ -48,7 +48,9 @@ namespace RLGSC {
 		void EpisodeReset(const GameState& initialState);
 		FList2 BuildObservations(const GameState& state);
 		FList GetRewards(const GameState& state, bool done);
-		bool IsDone(const GameState& state);
+		// RLbot patch (truncation): outTruncated is set to true when the episode ended ONLY
+		// because of truncation conditions (see TerminalCondition::IsTruncation).
+		bool IsDone(const GameState& state, bool* outTruncated = nullptr);
 		ScoreLine GetScoreLine(const GameState& state);
 		ActionSet ParseActions(const ActionParser::Input& actionsData, const GameState& gameState);
 		GameState ResetState(Arena* arena);
diff --git a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.cpp b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.cpp
index 50f688b..d59d626 100644
--- a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.cpp
+++ b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.cpp
@@ -89,7 +89,8 @@ namespace RLGSC {
 		}
 
 		FList2 obs = match->BuildObservations(state);
-		bool done = match->IsDone(state);
+		bool truncated = false;
+		bool done = match->IsDone(state, &truncated);
 		FList rewards = match->GetRewards(state, done);
 		prevState = state;
 
@@ -97,7 +98,8 @@ namespace RLGSC {
 			obs,
 			rewards,
 			done,
-			state
+			state,
+			truncated
 		};
 	}
 }
\ No newline at end of file
diff --git a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.h b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.h
index 311d3da..82ecb93 100644
--- a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.h
+++ b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Gym.h
@@ -1,6 +1,12 @@
 #pragma once
 #include "Envs/Match.h"
 
+// RLbot patch (truncation) applied: Gym::StepResult carries a truncated flag.
+#define RLGSC_HAS_TRUNCATION 1
+// RLbot patch (truncation, review R4): StepResult::finalObs holds the last observation of a
+// finished episode; truncated episodes bootstrap from it instead of the reset observation.
+#define RLGSC_HAS_FINAL_OBS 1
+
 namespace RLGSC {
 	class Gym {
 	public:
@@ -26,6 +32,13 @@ namespace RLGSC {
 			FList reward;
 			bool done;
 			GameState state;
+			// RLbot patch (truncation): done, but only because of a truncation condition.
+			// Kept last so existing aggregate initializers stay valid.
+			bool truncated = false;
+			// RLbot patch (truncation, review R4): the last observation of the finished episode.
+			// Only set by RLGPC::GameInst::Step when done, right BEFORE obs is replaced by the
+			// reset observation of the next episode; empty otherwise (Gym::Step never sets it).
+			FList2 finalObs = {};
 		};
 		virtual StepResult Step(const ActionParser::Input& actionsData);
 
diff --git a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Utils/TerminalConditions/TerminalCondition.h b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Utils/TerminalConditions/TerminalCondition.h
index 91bcad2..a5aa473 100644
--- a/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Utils/TerminalConditions/TerminalCondition.h
+++ b/RLGymPPO_CPP/RLGymSim_CPP/src/RLGymSim_CPP/Utils/TerminalConditions/TerminalCondition.h
@@ -6,5 +6,11 @@ namespace RLGSC {
 	public:
 		virtual void Reset(const GameState& initialState) {};
 		virtual bool IsTerminal(const GameState& currentState) = 0;
+
+		// RLbot patch (truncation): true if this condition ends the episode without the
+		// environment actually being terminal (time limits, no-touch timeouts). The learner then
+		// bootstraps the value of the final state instead of treating it as a terminal state.
+		// Terminal conditions like GoalScoreCondition keep the default (false).
+		virtual bool IsTruncation() const { return false; }
 	};
 }
\ No newline at end of file
diff --git a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgent.cpp b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgent.cpp
index 478d2b9..fd1aaaa 100644
--- a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgent.cpp
+++ b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgent.cpp
@@ -136,12 +136,24 @@ void _RunFunc(ThreadAgent* ta) {
 
 				auto& stepResult = stepResults[i];
 
-				float done = (float)stepResult.done;
-				float truncated = (float)false;
+				// RLbot patch (truncation): a timeout is not a terminal state. Store it as
+				// truncated (bootstrap from the final observation) instead of done.
+				float done = (float)(stepResult.done && !stepResult.truncated);
+				float truncated = (float)stepResult.truncated;
 
 				auto tDone = torch::tensor(done);
 				auto tTruncated = torch::tensor(truncated);
 
+				// For finished episodes, nextObsTensor (and stepResult.obs) already hold the RESET
+				// observation of the next episode (GameInst::Step resets the gym). The GAE needs
+				// the last observation of the finished episode to bootstrap a truncation
+				// correctly; GameInst::Step saved it in stepResult.finalObs before the reset
+				// (review R4: the first version of this patch read stepResult.obs here, i.e.
+				// bootstrapped timeouts from the reset observation).
+				torch::Tensor finalObsTensor;
+				if (stepResult.done)
+					finalObsTensor = FLIST2_TO_TENSOR(stepResult.finalObs);
+
 				for (int j = 0; j < numPlayers; j++) {
 					ta->trajectories[i][j].AppendSingleStep(
 						{
@@ -154,7 +166,7 @@ void _RunFunc(ThreadAgent* ta) {
 							torch::Tensor(),
 #endif
 
-							nextObsTensor[playerOffset + j],
+							stepResult.done ? finalObsTensor[j] : nextObsTensor[playerOffset + j],
 							tDone,
 							tTruncated
 						}
diff --git a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.cpp b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.cpp
index 9b128f6..0be420a 100644
--- a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.cpp
+++ b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.cpp
@@ -43,16 +43,35 @@ RLGPC::GameTrajectory RLGPC::ThreadAgentManager::CollectTimesteps(uint64_t amoun
 		// Combine all of their trajectories into one long trajectory
 		// We will return this giant trajectory to the learner
 		std::vector<GameTrajectory> trajs;
+		lastEnvTruncIdx.clear();
+		lastEnvTruncHasNext.clear();
 		for (auto agent : agents) {
 			agent->trajMutex.lock();
 			for (auto& trajSet : agent->trajectories) {
 				for (auto& traj : trajSet) {
 					if (traj.size > 0) {
+						// RLbot patch (truncation, review R4): remember where the environment itself
+						// truncated (timeouts), before the block boundary below is marked as well.
+						// Positions refer to the concatenated result (MultiAppend keeps this order).
+						{
+							torch::Tensor truncs = traj.data.truncateds.slice(0, 0, traj.size).contiguous();
+							const float* t = truncs.data_ptr<float>();
+							for (size_t i = 0; i < traj.size; i++) {
+								if (t[i] > 0.5f) {
+									lastEnvTruncIdx.push_back((int64_t)(totalTimesteps + i));
+									lastEnvTruncHasNext.push_back(i + 1 < traj.size);
+								}
+							}
+						}
+
 						// If the last timestep is not a done, mark it as truncated
 						// The GAE needs to know when the environment state stops being continuous
 						// This happens either because the environment reset (i.e. goal scored), called "done",
 						//	or the data got cut short, called "truncated"
-						traj.data.truncateds[traj.size - 1] = (traj.data.dones[traj.size - 1].item<float>() == 0);
+						// RLbot patch (truncation): OR instead of assignment, so a truncation already
+						// marked by the environment (timeout) is kept.
+						if (traj.data.dones[traj.size - 1].item<float>() == 0)
+							traj.data.truncateds[traj.size - 1] = 1.f;
 						trajs.push_back(traj);
 						totalTimesteps += traj.size;
 						traj.Clear();
diff --git a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.h b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.h
index 8476b28..99709d0 100644
--- a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.h
+++ b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Threading/ThreadAgentManager.h
@@ -30,6 +30,13 @@ namespace RLGPC {
 		double lastIterationTime = 0;
 		WelfordRunningStat obsStats;
 
+		// RLbot patch (truncation, review R4): environment truncations (timeouts, not block
+		// boundaries) of the last CollectTimesteps() call, as positions in the returned
+		// trajectory. hasNext: the following position belongs to the same game and player, its
+		// state is the reset observation of the next episode. Used for diagnostics only.
+		std::vector<int64_t> lastEnvTruncIdx;
+		std::vector<bool> lastEnvTruncHasNext;
+
 		ThreadAgentManager(
 			DiscretePolicy* policy, DiscretePolicy* policyHalf, ExperienceBuffer* expBuffer, 
 			bool standardizeOBS, bool deterministic, bool blockConcurrentInfer, uint64_t maxCollect, torch::Device device) :
diff --git a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.cpp b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.cpp
index 71eb714..4e20560 100644
--- a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.cpp
+++ b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.cpp
@@ -5,10 +5,13 @@
 void RLGPC::TorchFuncs::ComputeGAE(
 	const FList& rews, const FList& dones, const FList& truncated, const FList& values, 
 	torch::Tensor& outAdvantages, torch::Tensor& outValues, FList& outReturns, 
-	float gamma, float lambda, float returnStd, float clipRange
+	float gamma, float lambda, float returnStd, float clipRange,
+	const FList& truncNextValues
 ) {
 	auto next_values = FList(values.begin() + 1, values.end());
 	auto& terminal = dones;
+	bool useTruncNext = !truncNextValues.empty();
+	assert(!useTruncNext || truncNextValues.size() == rews.size());
 
 	float returnScale = 1 / returnStd;
 	if (isnan(returnScale))
@@ -33,7 +36,10 @@ void RLGPC::TorchFuncs::ComputeGAE(
 			norm_rew = rews[step];
 		}
 
-		float pred_ret = norm_rew + gamma * next_values[step] * done;
+		// RLbot patch (truncation): bootstrap a truncated step from the value of its own
+		// next state, not from the next entry of the concatenated trajectory list.
+		float nextValue = (useTruncNext && truncated[step] > 0.5f) ? truncNextValues[step] : next_values[step];
+		float pred_ret = norm_rew + gamma * nextValue * done;
 		float delta = pred_ret - values[step];
 		float ret = rews[step] + lastReturn * gamma * done * trunc;
 		returns[step] = ret;
diff --git a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.h b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.h
index 2a2ce17..e0b1c88 100644
--- a/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.h
+++ b/RLGymPPO_CPP/src/private/RLGymPPO_CPP/Util/TorchFuncs.h
@@ -6,10 +6,15 @@
 namespace RLGPC {
 	// https://github.com/AechPro/rlgym-ppo/blob/main/rlgym_ppo/util/torch_functions.py
 	namespace TorchFuncs {
-		void ComputeGAE(
+		// RLbot patch (truncation): truncNextValues[i] is the value of nextStates[i] and is used
+		// as the bootstrap value at every step where truncated[i] != 0 (instead of values[i + 1],
+		// which belongs to the next episode or even to another game's trajectory). Pass an
+		// empty list for the original behaviour. Exported so that tests can call it.
+		RG_IMEXPORT void ComputeGAE(
 			const FList& rews, const FList& dones, const FList& truncated, const FList& values,
 			torch::Tensor& outAdvantages, torch::Tensor& outValues, FList& outReturns,
-			float gamma = 0.99f, float lambda = 0.95f, float returnStd = 0, float clipRange = 10
+			float gamma = 0.99f, float lambda = 0.95f, float returnStd = 0, float clipRange = 10,
+			const FList& truncNextValues = {}
 		);
 
 		// torch::cat({a, b}, 0) but returns b.clone() if a is undefined
diff --git a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.cpp b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.cpp
index 923de67..fca7dc3 100644
--- a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.cpp
+++ b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.cpp
@@ -651,13 +651,43 @@ void RLGPC::Learner::AddNewExperience(GameTrajectory& gameTraj, Report& report)
 	
 	float retStd = (config.standardizeReturns ? returnStats.GetSTD()[0] : 1);
 
+	// RLbot patch (truncation): value of the next state at every truncated step, so that
+	// timeouts (and the boundaries between concatenated game trajectories) bootstrap from the
+	// correct state instead of from the next entry in the list.
+	FList truncatedList = TENSOR_TO_FLIST(trajData.truncateds);
+	FList truncNextValues;
+	{
+		std::vector<int64_t> truncIdx;
+		for (size_t i = 0; i < truncatedList.size(); i++)
+			if (truncatedList[i] > 0.5f)
+				truncIdx.push_back((int64_t)i);
+
+		if (!truncIdx.empty()) {
+			truncNextValues = FList(count, 0.f);
+			auto idxTensor = torch::tensor(truncIdx);
+			auto truncStates = trajData.nextStates.index_select(0, idxTensor);
+			auto truncValsTensor = torch::zeros({ (int64_t)truncIdx.size() });
+			for (int64_t i = 0; i < (int64_t)truncIdx.size(); i += ppo->config.miniBatchSize) {
+				int64_t end = RS_MIN(i + ppo->config.miniBatchSize, (int64_t)truncIdx.size());
+				auto part = ppo->valueNet->Forward(truncStates.slice(0, i, end).to(ppo->device, true, true)).cpu().flatten();
+				truncValsTensor.slice(0, i, end).copy_(part, true);
+			}
+			FList truncVals = TENSOR_TO_FLIST(truncValsTensor);
+			for (size_t k = 0; k < truncIdx.size(); k++)
+				truncNextValues[truncIdx[k]] = truncVals[k];
+			report["Truncated Steps"] = (int64_t)truncIdx.size();
+		} else {
+			report["Truncated Steps"] = 0;
+		}
+	}
+
 	// Compute GAE stuff
 	torch::Tensor advantages, valueTargets;
 	FList returns;
 	TorchFuncs::ComputeGAE(
 		TENSOR_TO_FLIST(trajData.rewards),
 		TENSOR_TO_FLIST(trajData.dones),
-		TENSOR_TO_FLIST(trajData.truncateds),
+		truncatedList,
 		valPreds,
 		advantages,
 		valueTargets,
@@ -665,7 +695,8 @@ void RLGPC::Learner::AddNewExperience(GameTrajectory& gameTraj, Report& report)
 		config.gaeGamma,
 		config.gaeLambda,
 		retStd,
-		config.rewardClipRange
+		config.rewardClipRange,
+		truncNextValues
 	);
 
 	float avgRet = 0;
@@ -677,6 +708,59 @@ void RLGPC::Learner::AddNewExperience(GameTrajectory& gameTraj, Report& report)
 	report["Avg Advantage"] = advantages.abs().mean().item<float>();
 	report["Avg Val Target"] = valueTargets.abs().mean().item<float>();
 
+	// RLbot patch (truncation, review R4): diagnostics for environment truncations (timeouts).
+	// Proves in metrics.csv that timeouts bootstrap from the last observation of the episode:
+	//   Timeout Truncations          count per player and iteration
+	//   Trunc Bootstrap Reset Share  share of timeouts whose bootstrap state IS the reset
+	//                                observation of the next episode (must be 0; was 1 before R4)
+	//   Trunc Bootstrap V Final/Reset/Diff  mean V(last obs), mean V(reset obs) and their
+	//                                difference, in the critic's (normalized) units
+	{
+		const auto& envIdx = agentMgr->lastEnvTruncIdx;
+		const auto& envHasNext = agentMgr->lastEnvTruncHasNext;
+		FList rewardList = truncationDiagnosticsCallback ? TENSOR_TO_FLIST(trajData.rewards) : FList();
+		FList advList = truncationDiagnosticsCallback ? TENSOR_TO_FLIST(advantages) : FList();
+		TruncationDiagnostics diag = {};
+		diag.gamma = config.gaeGamma;
+		diag.returnStd = retStd;
+		diag.rewardClipRange = config.rewardClipRange;
+
+		report["Timeout Truncations"] = (int64_t)envIdx.size();
+		int64_t pairs = 0, bootstrapIsReset = 0;
+		double sumFinal = 0, sumReset = 0;
+		for (size_t k = 0; k < envIdx.size(); k++) {
+			int64_t i = envIdx[k];
+			bool hasNext = envHasNext[k] && i + 1 < (int64_t)count;
+			float vBoot = truncNextValues.empty() ? valPreds[i + 1] : truncNextValues[i];
+			if (hasNext) {
+				pairs++;
+				sumFinal += vBoot;
+				sumReset += valPreds[i + 1];
+				if (torch::equal(trajData.nextStates[i], trajData.states[i + 1]))
+					bootstrapIsReset++;
+			}
+			if (truncationDiagnosticsCallback) {
+				diag.envTruncIdx.push_back(i);
+				diag.envTruncHasNext.push_back(hasNext);
+				diag.bootstrapStates.push_back(TENSOR_TO_FLIST(trajData.nextStates[i]));
+				diag.followingStates.push_back(hasNext ? TENSOR_TO_FLIST(trajData.states[i + 1]) : FList());
+				diag.bootstrapValues.push_back(vBoot);
+				diag.followingValues.push_back(hasNext ? valPreds[i + 1] : NAN);
+				diag.stateValues.push_back(valPreds[i]);
+				diag.rewards.push_back(rewardList[i]);
+				diag.advantages.push_back(advList[i]);
+			}
+		}
+		if (pairs > 0) {
+			report["Trunc Bootstrap Reset Share"] = (double)bootstrapIsReset / pairs;
+			report["Trunc Bootstrap V Final"] = sumFinal / pairs;
+			report["Trunc Bootstrap V Reset"] = sumReset / pairs;
+			report["Trunc Bootstrap V Diff"] = (sumFinal - sumReset) / pairs;
+		}
+		if (truncationDiagnosticsCallback)
+			truncationDiagnosticsCallback(diag);
+	}
+
 	if (config.standardizeReturns) {
 		int numToIncrement = RS_MIN(config.maxReturnsPerStatsInc, returns.size());
 		returnStats.Increment(returns, numToIncrement);
diff --git a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.h b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.h
index 0a648f5..0e3e3d1 100644
--- a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.h
+++ b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Learner.h
@@ -10,6 +10,23 @@ namespace RLGPC {
 
 	typedef std::function<void(class Learner*, Report&)> IterationCallback;
 
+	// RLbot patch (truncation, review R4): what the GAE of one iteration saw at the steps where
+	// the environment truncated (timeouts; block boundaries are not included). One entry per
+	// such step. Filled in Learner::AddNewExperience only if the callback is set (tests).
+	struct TruncationDiagnostics {
+		std::vector<int64_t> envTruncIdx;   // position in the iteration's trajectory
+		std::vector<bool> envTruncHasNext;  // the next position is the same game and player
+		FList2 bootstrapStates;             // nextStates[idx]: state the GAE bootstraps from
+		FList2 followingStates;             // states[idx + 1]: reset obs of the next episode (empty if !hasNext)
+		FList bootstrapValues;              // value used for the bootstrap in the GAE
+		FList followingValues;              // V(states[idx + 1]) = what the unpatched GAE used (NAN if !hasNext)
+		FList stateValues;                  // V(states[idx])
+		FList rewards;                      // raw reward of the step
+		FList advantages;                   // GAE advantage of the step
+		float gamma = 0, returnStd = 1, rewardClipRange = 0;
+	};
+	typedef std::function<void(const TruncationDiagnostics&)> TruncationDiagnosticsCallback;
+
 	// https://github.com/AechPro/rlgym-ppo/blob/main/rlgym_ppo/learner.py
 	class RG_IMEXPORT Learner {
 	public:
@@ -53,6 +70,8 @@ namespace RLGPC {
 
 		IterationCallback iterationCallback = NULL;
 		StepCallback stepCallback = NULL;
+		// RLbot patch (truncation, review R4)
+		TruncationDiagnosticsCallback truncationDiagnosticsCallback = NULL;
 
 		RG_NO_COPY(Learner);
 
diff --git a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Threading/GameInst.cpp b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Threading/GameInst.cpp
index fc2d367..077bfa4 100644
--- a/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Threading/GameInst.cpp
+++ b/RLGymPPO_CPP/src/public/RLGymPPO_CPP/Threading/GameInst.cpp
@@ -9,8 +9,6 @@ RLGSC::Gym::StepResult RLGPC::GameInst::Step(const IList& actions) {
 	// Step with agent actions
 	auto stepResult = gym->Step(actions);
 
-	auto& nextObs = stepResult.obs;
-
 	{ // Update avg rewards
 		float totalRew = 0;
 		for (int i = 0; i < match->playerAmount; i++)
@@ -25,13 +23,18 @@ RLGSC::Gym::StepResult RLGPC::GameInst::Step(const IList& actions) {
 
 	// Environment ending
 	if (stepResult.done) {
-		nextObs = gym->Reset();
-		
+		// RLbot patch (truncation, review R4): keep the last observation of the episode before
+		// obs is overwritten with the reset observation of the next episode. The ThreadAgent
+		// bootstraps truncated episodes (timeouts) from it. Moved, not copied, and only at
+		// episode ends: no cost in the normal step.
+		stepResult.finalObs = std::move(stepResult.obs);
+		stepResult.obs = gym->Reset();
+
 		avgEpRew += curEpRew;
 		curEpRew = 0;
 	}
 
-	curObs = nextObs;
+	curObs = stepResult.obs;
 	totalSteps++;
 
 	return stepResult;
